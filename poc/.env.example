# Solana (devnet)
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_NETWORK=devnet
# Chave privada base58 OU caminho para keypair JSON (nunca commitar o valor real)
SOLANA_PRIVATE_KEY=
SOLANA_KEYPAIR_PATH=

# Preço (SOL -> USD)
COINGECKO_API_URL=https://api.coingecko.com/api/v3/simple/price

# AWS (baseline S3)
AWS_REGION=us-east-1
AWS_PROFILE=eworks-dev
S3_BUCKET=colosseum-poc-benchmark
